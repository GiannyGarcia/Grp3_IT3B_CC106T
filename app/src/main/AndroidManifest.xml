<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SanIsidroPharmacy.NoActionBar">

        <!-- ✅ 1. Main Page (Launcher / Welcome Screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ 2. Login Screen -->
        <activity android:name=".LoginActivity" />

        <!-- ✅ 3. Sign Up Screen -->
        <activity android:name=".SignUpActivity" />

        <!-- ✅ 4. Main Catalog / Pharmacy UI -->
        <activity android:name=".CatalogActivity" />

        <!-- Other screens (keep as-is) -->
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".MedicineDetailActivity" />
        <activity android:name=".CategoryDetailsActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".EditProfileActivity" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
