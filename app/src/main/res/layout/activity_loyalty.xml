<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="#FAFAFA"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- TITLE -->
    <TextView
        android:id="@+id/textTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Loyalty Rewards"
        android:textSize="28sp"
        android:textStyle="bold"
        android:padding="20dp"
        android:textColor="#000" />

    <!-- MAIN CONTENT (fills space) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="20dp">

            <!-- POINTS -->
            <TextView
                android:id="@+id/textPoints"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Loading..."
                android:textStyle="bold"
                android:textSize="26sp" />

            <!-- TIER BADGE -->
            <ImageView
                android:id="@+id/tierBadge"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_marginTop="20dp"
                android:src="@drawable/ic_user" />

            <!-- Tier Label -->
            <TextView
                android:id="@+id/textTier"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tier: Bronze"
                android:paddingTop="12dp"
                android:textSize="18sp"
                android:textColor="#444"/>

            <!-- PROGRESS LABEL -->
            <TextView
                android:id="@+id/textProgressLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Progress to next tier:"
                android:paddingTop="20dp"
                android:textSize="16sp"/>

            <!-- PROGRESS BAR -->
            <ProgressBar
                android:id="@+id/progressPoints"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="12dp"
                android:progress="0"
                android:max="100"
                android:progressTint="#FF0000"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="20dp" />

            <!-- Tier Requirements -->
            <TextView
                android:id="@+id/textNextTier"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Earn 100 points to reach SILVER"
                android:textSize="16sp" />

        </LinearLayout>
    </ScrollView>

    <!-- BOTTOM NAV ALWAYS AT BOTTOM -->
    <include
        android:id="@+id/include_bottom_nav"
        layout="@layout/include_bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="60dp"/>

</LinearLayout>
